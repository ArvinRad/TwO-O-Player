# This is the main entrypoint into the game
# It requires the other files/gems that it needs
require './Players_Module'
require './Math_Module'

## My test code can go here
puts qualified_candidates(@candidates);
puts sorted_candidates(@candidates);
 pp @candidates
